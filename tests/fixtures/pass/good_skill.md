---
name: fetch_weather
version: "1.0.0"
description: Fetches current weather data for a given city from the OpenWeatherMap API and returns temperature, humidity, and conditions as structured JSON.
---

## Usage

Call this skill with a `city` parameter. The skill queries the API and returns a structured response.

## Parameters

- `city` (string, required): The city name to fetch weather for.
